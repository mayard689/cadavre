<!DOCTYPE html>
<html class="xscroll-container">
    <head >
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('cadavre') }}
        {% endblock %}

        <link rel="preload" href="{{ asset('build/fonts/Playlist Script.otf') }}" as="font" crossOrigin="anonymous" />
        <link rel="preload" href="{{ asset('build/fonts/Lato-Regular.woff2') }}" as="font" crossOrigin="anonymous" />
        <link rel="preload" href="{{ asset('build/fonts/Poppins-Regular.woff2') }}" as="font" crossOrigin="anonymous" />

        <link rel="apple-touch-icon" sizes="144x144" href="favicon/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="favicon/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="favicon/favicon-16x16.png">
        <link rel="manifest" href="favicon/site.webmanifest">
        <link rel="mask-icon" href="favicon/safari-pinned-tab.svg" color="#5bbad5">
        <meta name="msapplication-TileColor" content="#da532c">
        <meta name="theme-color" content="#ffffff">

    </head>
    <body class="parallax-container1">
        <div class="flashes">
            {% for category, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ category }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        {% block body %}{% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('cadavre') }}
        {% endblock %}

    </body>
</html>
