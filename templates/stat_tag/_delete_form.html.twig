<form method="post" action="{{ path('stat_tag_delete', {'id': stat_tag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stat_tag.id) }}">
    <button class=" btn btn-primary">Delete</button>
</form>
