{% extends 'base.html.twig' %}

{% block title %}StatTag{% endblock %}

{% block body %}
    <h1>StatTag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stat_tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stat_tag.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ stat_tag.date ? stat_tag.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Sessionid</th>
                <td>{{ stat_tag.sessionid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stat_tag_index') }}">back to list</a>

    <a href="{{ path('stat_tag_edit', {'id': stat_tag.id}) }}">edit</a>

    {{ include('stat_tag/_delete_form.html.twig') }}
{% endblock %}
