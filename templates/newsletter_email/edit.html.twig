{% extends 'base.html.twig' %}

{% block title %}Edit NewsletterEmail{% endblock %}

{% block body %}
    <h1>Edit NewsletterEmail</h1>

    {{ include('newsletter_email/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('newsletter_email_index') }}">back to list</a>

    {{ include('newsletter_email/_delete_form.html.twig') }}
{% endblock %}
