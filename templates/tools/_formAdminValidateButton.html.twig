{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUBSCRIBER') %}
    <button class="btn btn-sm btn-secondary mt-2 mr-2">
        <i class="fas fa-check mr-1"> </i>
        {{ text??"Enregistrer" }}
    </button>
{% endif %}
