{% extends 'base.html.twig' %}

{% block title %}Réglisse au Pays des Vermeils!{% endblock %}

{% block body %}

    <div class="container">

        <h1>Réglisse au Pays des Vermeils!</h1>
        <p class="h1-sub">Un cadavre Exquis avec le <span class="cdf">Comité des Fêtes de Villereau</span></p>

        <section class="previous">
            Reprenons ou en est resté le précédant joueur ...

            {% for sentence in sentences %}
                <p class="user-entry">{{ sentence.text }}</p>
            {% endfor %}
        </section>

        <section class="next">
            Et d'aprés vous, que se passe-t-il ensuite ?

            {{ form_start(form) }}
            {{ form_widget(form.text) }}
            {{ form_errors(form.text) }}

            {{ form_widget(form.chapter, { 'attr': {'class': 'd-none'} }) }}
            {{ form_errors(form.chapter, { 'attr': {'class': 'd-none'} }) }}
            <button class="btn btn-sm btn-primary"> Enregistrer </button>
            {{ form_end(form) }}
        </section>


    </div>

{% endblock %}
