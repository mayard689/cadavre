<section class="container-fluid">
    <div class="container">



        <div class="row">
            <div class="blog-row col-12 col-md-6 ">
                {%  include 'content/_contentCard3.html.twig' with {
                    'content' : mainContent,
                } %}
            </div>

            <div class="blog-row col-12 col-md-6 ">
                <div class="row">
                    {% for content in contents %}
                        {% include 'content/_contentCard2.html.twig' %}
                    {% endfor %}
                </div>
            </div>
        </div>


    </div>
</section>

<section class="container">
    <div class="row m-5">
        <button type="button" class="btn btn-primary align-self-center" data-toggle="modal" data-target="#newsletterModal">
            Recevoir la newsletter
        </button>
    </div>

    <div class="row m-5">
        {{ render(path('weather')) }}
    </div>
</section>
